name: CI/CD Pipeline

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Add build steps here (e.g., compiling code, running tests)

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install SSH client
        run: sudo apt install openssh-client

      - name: echo secrets
        run : echo '${{ secrets.SSH_KEY }}'

      - name: echo secrets
        run : echo '${{ secrets.EC2_IP_ADDRESS }}'

      - name: SSH into DigitalOcean server
        run: echo '${{ secrets.SSH_KEY }}' | ssh -o StrictHostKeyChecking=no -i /dev/stdin username@${{ secrets.EC2_IP_ADDRESS }} 'cd /var/www/html && git pull origin master'

      - name: Install PHP dependencies
        run: composer install

      - name: Deploy code
        run: npm run build
